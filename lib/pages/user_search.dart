import 'package:flutter/material.dart';

class PageUserSearch extends StatelessWidget {
  const PageUserSearch({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SizedBox(
        child: Text("js"),
      ),
    );
  }
}